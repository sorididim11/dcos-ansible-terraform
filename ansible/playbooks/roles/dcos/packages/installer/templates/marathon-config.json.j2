
{
  "service": {
    "name": "marathon-{{item.0.name}}"
  },
  "marathon": {
     "default-accepted-resource-roles": "{{item.1.name}}",
     "mesos-authentication": true,
     "mesos-authentication-principal": "{{item.0.name}}-id",
     "mesos_authentication_secret": "{{item.0.package}}/{{item.0.name}}",
     "mesos-role": "{{item.1.name}}",
     "mesos-user": "{{item.0.user}}"
     }
} 
