
{
  "service": {
    "name": "{{service.name}}"
  },
  "marathon": {
     "default-accepted-resource-roles": "{{service.role.name}}",
     "mesos-authentication": true,
     "mesos-authentication-principal": "{{service.name}}",
     "mesos-authentication-secret": "{{service.name}}/{{service.secret_name | default('secret')}}",
     "mesos-role": "{{service.role.name}}",
     "mesos-user": "{{service.user}}"
     }
} 
