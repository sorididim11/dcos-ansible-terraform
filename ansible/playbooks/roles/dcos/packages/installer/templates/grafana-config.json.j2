{
  "service": {
    "name": "{{item.name}}"
  },
  "grafana": {
    "cpus": 0.3,
    "mem": 512,
    "admin_password": "{{ item.options.password | default('admin') }}"
  },
  "networking": {
    "external_access": {
      "enable": true,
      "external_access_port": 13000,
      "virtual_host": "grafana.example.org"
    }
  }
}