{  
   "service":{  
      "name": "{{item.name}}",
      "principal": "{{item.name}}",
      "secret": "{{item.name}}/{{item.secrete_name | default('secret')}}",
      {% if edcos_security_mode == 'strict' %}"user": "{{item.user}}", {% endif %}
      "role": "{{item.role.name}}"
   }
}