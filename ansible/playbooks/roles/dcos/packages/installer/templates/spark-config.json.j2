{  
   "service":{  
      "name": "{{service.name}}",
      "principal": "{{service.name}}",
      "secret": "{{service.name}}/{{service.secret_name | default('secret')}}",
      {% if edcos_security_mode == 'strict' %}"user": "{{service.user}}", {% endif %}
      "role": "{{service.role.name}}"
   }
}