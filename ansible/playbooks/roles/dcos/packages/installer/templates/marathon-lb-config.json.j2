{
  "marathon-lb": {
    "name": "{{service.name}}",
    "principal": "{{service.name}}",
    "secret_name": "{{service.name}}/{{service.secret_name | default('secret')}}",
    "marathon-uri": "https://marathon.mesos:8443"
  }
}