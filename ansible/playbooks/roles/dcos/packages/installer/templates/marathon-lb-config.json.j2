{
  "marathon-lb": {
    "name": "{{item.name}}",
    "principal": "{{ item.account_id | default(item.name) }}",
    "secret_name": "{{ item.account_sec | default(item.name + '/secret') }}",
    "marathon-uri": "https://marathon.mesos:8443"
  }
}