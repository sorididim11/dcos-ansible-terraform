{
  "marathon-lb": {
    "name": "{{item.name}}",
    "principal": "{{item.name}}",
    "secret_name": "{{item.name}}/{{item.secret_name | default('secret')}}",
    "marathon-uri": "https://marathon.mesos:8443"
  }
}