--- 

- include: pkg-install-marathon-lb.yml

- name: Install cadvisor
  gather_facts: False
  hosts: dcos_cli[0]
  vars_files: 
    - pkg-desc/monitoring.yml
  tags: ['pkg']
  roles:
    - { role: dcos/packages/installer, service: "{{services[0]}}" }
    - { role: dcos/packages/installer, service: "{{services[1]}}" }
    - { role: dcos/packages/installer, service: "{{services[2]}}" }
  environment: "{{ proxy_env }}"