---
- name: Test module
  connection: local
  hosts: localhost
  gather_facts: False
  vars_files: 
     - library/role_def.yml
  vars: 
    nodes: "{{ lookup('file','library/node.json') | from_json }}"
  tasks:
  - name: test mesos 
    mesos_dynamic_reserve:
      url: http://hello
      token: 12345
      mesos_role: "{{services['marathon-ee-dev'].role}}"
      nodes_status: "{{nodes}}"
 