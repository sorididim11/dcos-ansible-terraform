--- 

- name: "Install Elastic Logging platform"
  hosts: all:!dcos_bootstrap   #ansible partition to excluse bootstrap nodes
  gather_facts: False
  become: yes
  roles:
    - { role: dcos/packages/logging } 
  environment: "{{ proxy_env }}"
